{
  "name": "acerola-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Acerola AI personal intelligence system",
  "scripts": {
    "android:build": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
